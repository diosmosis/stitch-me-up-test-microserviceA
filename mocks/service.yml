namespace: microserviceA

public:
  - web

services:
  web:
    image: node:4
    ports:
      - "3000:3000"
    volumes:
      - ..:/microserviceA
    working_dir: /microserviceA
    command: [npm, start]

setup:
  - cd .. && npm install
